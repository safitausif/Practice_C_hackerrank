# Practice_C_hackerrank
#include<stdio.h>
int update(int *p,int *q)
{
    int a,b;
    a=*p+*q;
    b=*p-*q;
    if(b<0)
    {
        b=b*(-1);
    }
    printf("%d\n%d",a,b);
    return 0;
}
int main()
{
    int a,b;
    scanf("%d\n%d",&a,&b);
    update(&a,&b);
    return 0;
}
